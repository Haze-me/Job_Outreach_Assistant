# Core framework
Django>=5.1,<5.3
djangorestframework>=3.15,<3.17

# Authentication
djangorestframework-simplejwt>=5.3,<6.0
argon2-cffi>=23.1  # backs the Argon2 password hasher configured in settings

# API tooling
django-filter>=24.3
drf-spectacular>=0.27,<0.29
django-cors-headers>=4.4

# Configuration
django-environ>=0.11

# Database drivers (SQLite is built in; psycopg is used when DATABASE_URL points at Postgres)
psycopg[binary]>=3.2

# Background processing
celery>=5.4,<6.0
redis>=5.0

# Crawling / parsing
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.3
